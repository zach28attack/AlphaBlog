<div class="container">
  <%= form_with(model: @user, local: true)  do |f| %>
    <div class="user-form hidden" id="session-form">
      <%= f.label :email%>
      <%= f.email_field :email, placeholder: "example@email.com"  %>
      <%= f.label :password%>
      <%= f.password_field :password, placeholder: "'password'" %>
      <%= f.submit "✅"%>
    </div>
  <%end%>
</div>
